#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.sync_with_stdio(0);
    cin.tie(0);
    int r1;
    double s;
    cin >> r1 >> s;

    //r1 + r2 / 2 = s
    //2s - r1 = r2;

    cout << 2 * s - r1;
}